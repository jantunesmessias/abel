schemaVersion: 1
kind: GatewayPreset
metadata:
  id: showcase-failure
spec:
  scopeId: showcase-api
  description: Deterministic unexpected API failure for diagnostic review
  backendMode: isolated
  routeIds:
    - dashboard-failure
  initialState:
    mode: failure
