schemaVersion: 1
kind: GatewayPreset
metadata:
  id: showcase-failure
spec:
  scopeId: showcase-api
  description: Deterministic unavailable API response for recovery review
  backendMode: isolated
  routeIds:
    - dashboard-failure
  initialState:
    mode: failure
