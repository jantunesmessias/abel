name: devex_engine
version: 0.1.0-dev
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  devex_contracts: ^0.1.0-dev
  meta: ^1.16.0
  yaml: ^3.1.3

dev_dependencies:
  test: ^1.26.3
