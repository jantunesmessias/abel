name: devex_flutter
version: 0.1.0-dev
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  devex_contracts: ^0.1.0-dev
  flutter:
    sdk: flutter
  http: ^1.6.0
  web: ^1.1.1

dev_dependencies:
  devex_runtime: ^0.1.0-dev
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: dev.devexkit.flutter
        pluginClass: DevExFlutterPlugin
